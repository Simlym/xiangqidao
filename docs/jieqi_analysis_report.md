# 揭棋功能深度对比分析：xiangqidao vs JieqiBox

> 分析日期：2026-08-30  
> 分析范围：揭棋（Jieqi）对弈、分析、引擎、记谱、交互等核心功能模块

---

## 一、项目概览

| 维度 | xiangqidao（当前项目） | JieqiBox |
|------|----------------------|----------|
| **技术栈** | React 18 + Tauri + Python FastAPI 后端 | Vue 3 + TypeScript + Tauri（纯前端+Rust） |
| **揭棋核心** | `frontend/src/core/game/jieqi.js`（421行，纯JS） | `src/composables/useChessGame.ts`（3356行，Vue Composable） |
| **引擎架构** | 三层降级：Native → WASM → 云端远程 | 本地 UCI 引擎（Pikafish）+ JAI  tournament 引擎 |
| **数据持久化** | 云端后端（用户系统、复盘库、积分、闯关） | 本地文件（棋谱 JSON、配置、FEN） |
| **目标用户** | 在线象棋学习平台用户（含训练、闯关、教练） | 揭棋桌面工具玩家和引擎开发者 |
| **国际多语言** | 中文为主 | 12 种语言（含简中、繁中、英、日、越、韩、俄等） |
| **测试覆盖** | `jieqi.test.js`（11个测试用例） | 无明显单元测试文件 |

---

## 二、揭棋规则引擎对比

### 2.1 核心规则实现

| 功能点 | xiangqidao | JieqiBox |
|--------|-----------|----------|
| **FEN 格式** | 自定义新格式：`board side hidden captured halfmove fullmove` | 支持新旧格式切换，兼容 Pikafish 旧格式 |
| **暗子初始分布** | 固定：`xxxxkxxxx/9/1x5x1/...` | 同样固定，通过 `getRoleByPosition` 映射 |
| **暗子移动规则** | 按初始位置角色移动（暗仕可斜走、暗马可日字等） | 相同规则 |
| **士/仕暗子限制** | 禁止 `d9c8`、`f9g8`、`d0c1`、`f0g1` 四个角 | 未显式发现同等限制（需进一步确认） |
| **随机数生成** | `Math.random` | Mersenne Twister（种子基于时间） |
| **有限知识 FEN** | `generateLimitedKnowledgeFen()` — 将被吃暗子放回池中 | `calculateDualPools()` — God View + Engine View 双池系统 |

### 2.2 关键差异：有限知识引擎（信息隐藏）

**xiangqidao** 的实现较为简洁：
- 被对手吃掉的暗子身份对引擎不可见 → 直接把这些身份放回暗子池
- 通过 `perspective` 参数区分应用着法时的信任级别
- 引擎着法若带扩展字符（指定翻子身份），在应用到权威局面时会重新从真实池抽取

**JieqiBox** 的实现更为复杂和精细：
- **双池系统**：`godViewPool`（真实状态）和 `engineViewPool`（引擎视角）
- **遍历历史记录**：逐条解析历史着法，判断哪些暗子是被人类吃掉的，然后把这些暗子加回引擎视角的池中
- **区分明吃和暗吃**：通过 `checkBasicCaptureFromHistory` 检查目标位置是否有明子，避免误将吃明子的情况加回池
- **支持 position-edit 后的回溯**：找到最近的位置编辑操作，从该点之后开始重新计算

> **评价**：JieqiBox 的有限知识引擎设计更严谨，特别在处理历史回溯和边界情况（如位置编辑后）时更加可靠。xiangqidao 的简化实现虽然代码更少，但在复杂场景（如导入棋谱后跳转到中间局面）可能存在信息泄露风险。

---

## 三、游戏模式对比

| 模式 | xiangqidao | JieqiBox |
|------|-----------|----------|
| **人机对战** | ✅ 执红/执黑，三级难度（6/10/14 depth），引擎自动走子 | ✅ 类似，支持选择 AI 执红/黑/双方 |
| **自由翻子** | ✅ 双方面对面走棋，手动选择暗子身份 | ✅ 类似，支持手动翻子 |
| **比赛模式** | ❌ 无 | ✅ JAI 引擎自动 tournament，支持多局对弈、WLD 统计、ELO 计算 |
| **AI 自对弈** | ❌ 无 | ✅ 可让红/黑方 AI 自动走子（"红方AI"/"黑方AI"开关） |
| **Ponder 模式** | ❌ 无 | ✅ 引擎预读对手走子，ponderhit/pondermiss 处理 |

### 3.1 比赛模式（JieqiBox 独有）

JieqiBox 的 `useJaiEngine.ts` 实现了完整的 tournament 系统：
- 多局自动对弈（`startmatch` 命令）
- 实时 WLD（胜-负-和）统计
- ELO 评分计算器
- 引擎配置持久化（`useConfigManager`）
- 每局结束后自动加载新 FEN 开始下一局

> **建议**：xiangqidao 若定位为训练平台，比赛模式可用于引擎测试和 AI 对弈直播场景。

---

## 四、引擎与分析系统对比

### 4.1 引擎架构

| 维度 | xiangqidao | JieqiBox |
|------|-----------|----------|
| **引擎类型** | Pikafish Jieqi 分支（UCI） | 同样 Pikafish Jieqi 分支 |
| **本地引擎** | ✅ PC Native 可执行文件 | ✅ UCI 可执行文件 |
| **WASM 引擎** | ✅ 浏览器内运行 | ❌ 未发现 |
| **云端引擎** | ✅ FastAPI 流式 SSE 回退 | ❌ 纯本地 |
| **引擎加载验证** | 通过 `jieqi_engine.py` 管理共享实例 | `uciok` / `jaiok` 信号验证 + 超时检测 |
| **自动加载** | 启动时检测可用引擎类型 | 自动加载上次使用的引擎 |

### 4.2 分析功能

| 功能 | xiangqidao | JieqiBox |
|------|-----------|----------|
| **实时分析** | ✅ 流式 SSE，movetime/depth/infinite | ✅ 本地 UCI 输出解析 |
| **MultiPV** | ✅ 支持 1/3/5 线 | ✅ 支持多线，可点击切换 |
| **评估条** | ✅ 竖向（棋盘左侧） | ✅ 横向/竖向可选 |
| **WDL 显示** | ✅ 胜率条 | ✅ WDL 彩色分段条 |
| **走子提示** | ✅ 虚线箭头标注 | ✅ 多箭头 + 序号标注 |
| **变着分析** | ❌ 无 | ✅ 排除某着法后重新分析 |
| **用户画图分析** | ❌ 无 | ✅ 右键画箭头后限定引擎搜索范围 |
| **搜索范围限定** | ✅ `searchMoves` 单线限定 | ✅ `searchmoves` 多线限定 |
| **分析参数** | 时间/深度/无限三选一 | 时间/深度/节点/最大思考时间/高级脚本 |
| **高级脚本** | ❌ 无 | ✅ 自定义 JavaScript 脚本动态计算搜索参数 |
| **Ponder** | ❌ 无 | ✅ go ponder + ponderhit |

### 4.3 引擎日志与调试

| 功能 | xiangqidao | JieqiBox |
|------|-----------|----------|
| **UCI 终端** | ❌ 无 | ✅ UciTerminalDialog，可手动发送命令 |
| **引擎输出日志** | `EngineAnalysisView` 组件显示 | 可折叠面板显示 sent/recv 行 |
| **输出节流** | 后端流式处理 | 前端 50ms 节流 + 将杀时 300ms 减速 |
| **内存清理** | 后端管理 | 自动清理：超过 1000 行截断至 500 行 |

---

## 五、棋谱与记谱系统对比

### 5.1 记谱能力

| 功能 | xiangqidao | JieqiBox |
|------|-----------|----------|
| **中文记谱** | ✅ `jieqiMoveToChinese`：含翻子/吃子后缀 | ✅ `uciToChineseMoves`：标准中文记谱 |
| **UCI 扩展格式** | ✅ 4位 + 翻子字符 + 吃子字符 | ✅ 类似扩展格式解析 |
| **棋谱保存** | ✅ 自动保存到云端复盘库 | ✅ 本地 JSON 文件导出/导入 |
| **棋谱回放** | ✅ 通过复盘系统（全局） | ✅ 内置播放/暂停/跳转（`goToFirst`/`goToLast`） |
| **自动播放** | ❌ 无 | ✅ 带速度控制的自动播放 |
| **走子注释** | ❌ 无 | ✅ `!!`/`!`/`!?`/`?!`/`?`/`??` 质量标记 |
| **富文本评论** | ❌ 无 | ✅ 每步独立评论，支持 Markdown 格式（粗体、斜体、链接等） |
| **变着分支** | ❌ 无 | ✅ 支持棋谱变着（通过排除着法重新分析生成） |
| **吃子历史** | ❌ 无 | ✅ `CaptureHistoryPanel`，人机模式下显示被吃暗子追踪 |
| **XQF 导入** | ❌ 无 | ✅ `convertXQFToJieqiNotation` |

### 5.2 棋谱数据结构

**xiangqidao** 的棋谱较为简单：
```json
{
  "variant": "jieqi",
  "initial_fen": "...",
  "moves": "a3a4A g3g4N ...",
  "positions": ["fen1", "fen2", ...],
  "result": "红胜",
  "source": "揭棋人机对弈"
}
```

**JieqiBox** 的棋谱结构更丰富：
```typescript
interface GameNotation {
  metadata: {
    event, site, date, round, white, black, result,
    initialFen, flipMode, currentFen, openingComment
  };
  moves: HistoryEntry[]; // 每条含 fen, comment, engineScore, annotation 等
}
```

---

## 六、交互与UI对比

### 6.1 棋盘交互

| 功能 | xiangqidao | JieqiBox |
|------|-----------|----------|
| **棋盘渲染** | SVG 网格 + DOM 棋子（CSS 定位） | SVG 底板 + `img` 棋子（百分比定位） |
| **走子方式** | 点击式（先点后点） | 点击式 |
| **棋子动画** | ✅ CSS 滑动动画（`--dx`/`--dy`） | ✅ CSS transition 0.2s |
| **翻转棋盘** | ✅ | ✅ |
| **自适应缩放** | ✅ 宽度+高度双重约束 | 容器自适应 |
| **右键画图** | ❌ 无 | ✅ 右键画箭头/圈，支持翻转同步 |
| **坐标显示** | ✅ 黑方数字+红方汉字 | ✅ 可选开关 |
| **棋子风格** | 皮肤系统（cosmetics.js） | 默认+国际化两种风格 |
| **主题系统** | 多主题棋盘+棋子 | Vuetify 暗色/亮色模式 |

### 6.2 暗子池显示

**xiangqidao**：
- 棋盘上下方各显示一方暗子池
- 显示：待揭数量 + 被吃暗子数量
- 纯展示，不可交互

**JieqiBox**：
- 棋盘上下方 `DarkPiecePoolStrip`
- 显示：待揭数量（badge）+ 被吃暗子（灰显）
- **可交互调整**：点击弹出卡片，可 ± 调整暗子池数量（用于手动修正或假设分析）
- 池状态验证：若暗子数不对会显示警告图标

> **关键差异**：JieqiBox 的暗子池支持手动调整，这在假设分析、教学、以及修正导入局面时非常有用。

### 6.3 弹窗与对话框

| 对话框 | xiangqidao | JieqiBox |
|--------|-----------|----------|
| 翻子选择 | 中间 modal 覆盖层 | 内嵌式选择 |
| 游戏结束 | 状态栏文字 | `GameEndDialog` |
| 设置 | 全局 Settings 页面 | 多个专用对话框（UCI/时间/界面/位置编辑） |
| 位置编辑 | ❌ 无 | ✅ `PositionEditorDialog`，可视化摆棋 |
| FEN 输入 | ❌ 无 | ✅ `FenInputDialog`，支持粘贴 |
| 开局库 | ❌ 无（标准象棋有） | ✅ `OpeningBookDialog` |
| 复盘分析 | ❌ 无 | ✅ `ReviewAnalysisDialog` |
| ELO 计算 | ❌ 无 | ✅ `EloCalculatorDialog` |
| PV 预览 | ❌ 无 | ✅ `PvPreviewDialog`，点击 PV 走子可预览 |

---

## 七、特色功能对比（JieqiBox 独有 vs xiangqidao 独有）

### 7.1 JieqiBox 独有功能

| 功能 | 价值说明 |
|------|---------|
| **运气指数 (Luck Index)** | 基于翻子序列计算当前局势运气偏向，增加娱乐性和数据分析维度 |
| **吃子历史追踪 (CaptureHistoryPanel)** | 人机模式下追踪被吃暗子身份，帮助玩家推理 |
| **位置编辑器** | 可视化摆棋，支持任意局面设置，对教学和研究至关重要 |
| **开局库 (Opening Book)** | 内置开局数据库，显示当前局面的开局名称和常见应手 |
| **变着分析** | 排除引擎推荐的首选着法，寻找次优选择，深入挖掘局面 |
| **用户画图分析** | 将手绘箭头转化为引擎搜索范围，直观且强大 |
| **Ponder 预读** | 引擎在对手思考时提前计算，响应更快 |
| **高级脚本引擎** | 用 JS 脚本动态控制搜索深度/时间/节点，专业级定制 |
| **图像识别摆棋** | `useImageRecognition` — 从截图自动识别棋盘摆棋 |
| **多语言 (12种)** | 国际化成熟，便于海外推广 |

### 7.2 xiangqidao 独有功能

| 功能 | 价值说明 |
|------|---------|
| **云端复盘系统** | 棋局自动同步云端，跨设备访问，长期保存 |
| **云端引擎 fallback** | 无本地引擎时自动降级到云端，零配置即可使用 |
| **WASM 引擎** | 浏览器内直接运行，无需安装任何引擎 |
| **用户系统 + 积分** | 会员体系、签到积分、外观商店 |
| **训练系统** | 残局训练、闯关模式、AI 教练 |
| **ELO 评分体系** | 完整的等级分计算和排行榜 |
| **SRS 间隔重复** | 错题自动复习系统 |
| **棋谱分析服务** | 云端自动全盘分析，生成报告 |
| **LLM 解释** | AI 解释棋局和训练题 |

---

## 八、从揭棋产品经理角度的评估

### 8.1 xiangqidao 揭棋模块的当前定位

xiangqidao 的揭棋功能目前定位为**平台的附属玩法**，核心价值在于：
- 让已有用户多一种娱乐方式
- 利用已有云端基础设施（引擎、复盘、用户系统）快速支持
- 作为差异化功能吸引揭棋爱好者

但揭棋模块本身更像一个**最小可行产品 (MVP)**，实现了核心对弈和分析，但在深度揭棋体验上还有较大提升空间。

### 8.2 JieqiBox 的产品深度

JieqiBox 是**揭棋专用工具**，产品在揭棋场景下的打磨深度明显更高：
- 每个功能都围绕揭棋的独特机制（暗子、翻子、信息隐藏）设计
- 分析工具链完整（从实时分析 → 变着挖掘 → 假设调整 → 报告输出）
- 社区和开发者友好（UCI 终端、引擎管理、JAI 比赛）

---

## 九、增强与改善建议

以下按优先级和模块分类，提出针对 xiangqidao 揭棋功能的增强建议。

### 🔴 P0 — 核心体验（建议尽快实现）

#### 1. 暗子池交互调整（参考 JieqiBox）
**问题**：当前暗子池是纯展示，玩家无法手动调整。在自由翻子模式下，如果用户想进行"假设分析"（"如果这里是车会怎样"），完全无法实现。

**建议**：
- 在自由翻子模式的分析面板中，增加暗子池手动调整功能
- 点击暗子图标弹出 ± 调整器
- 调整后重新触发引擎分析
- 调整状态不影响真实棋局，仅用于分析（类似 JieqiBox 的 `adjustUnrevealedCount`）

#### 2. 位置编辑器（FEN 摆棋）
**问题**：无法自定义开局或研究特定残局。揭棋的残局研究是高级玩家的核心需求。

**建议**：
- 增加可视化摆棋界面
- 支持从任意 FEN 开始揭棋对弈
- 摆棋时暗子池自动同步计算（`validationStatus` 逻辑可参考 JieqiBox）
- 支持粘贴 FEN 字符串

#### 3. 棋谱回放与分支（参考 JieqiBox 的 Notation 系统）
**问题**：当前棋谱只有线性列表，无法跳转、无法加注释、无法分叉。

**建议**：
- 棋谱列表支持点击跳转到任意步
- 支持每步添加文字评论
- 支持在任意步分支出变着（"如果当时走这里…"）
- 导出为标准化棋谱格式（兼容 JieqiBox 的 JSON 格式）

#### 4. 吃子历史面板（人机模式）
**问题**：人机对弈时，玩家不知道已经被吃掉了哪些暗子，推理依据不足。

**建议**：
- 在人机模式侧边栏增加"已吃暗子"追踪
- 显示：我方吃掉的暗子（已知）+ 对方吃掉的暗子（未知，仅显示数量）
- 帮助玩家进行概率推理

---

### 🟡 P1 — 分析增强（提升专业度）

#### 5. 变着分析（Exclude-Move Analysis）
**问题**：引擎只给最优着法，但揭棋中有时需要看"如果不走这步，次优是什么"。

**建议**：
- 在分析面板增加"排除此着"按钮
- 排除后自动重新分析，限定 `searchMoves` 为剩余着法
- 记录排除历史，可随时恢复

#### 6. 用户画图 + 画图分析
**问题**：无法直观标记棋盘上的关注点。

**建议**：
- 支持右键/长按画箭头、圈
- 支持"分析我的画图"：将箭头转化为引擎 `searchMoves`
- 画图随棋盘翻转同步变换

#### 7. 运气指数（Luck Index）
**问题**：揭棋有很强的运气成分，但玩家无法量化自己的运气。

**建议**：
- 基于翻子序列计算运气指数
- 公式思路：比较实际翻出的棋子价值与期望值
  - 翻出车的期望值高，翻出兵的期望值低
  - 红方翻出好车 = 运气好，反之运气差
- 在棋谱回放中显示每局的运气曲线

#### 8. Ponder 模式
**问题**：引擎仅在轮到它时才思考，响应有延迟。

**建议**：
- 人机模式下，引擎在对手走子时预读（`go ponder`）
- 如果对手实际走的是预读的那一步，立即 `ponderhit` 转为正式思考
- 如果不同，停止 ponder 重新开始

---

### 🟢 P2 — 内容生态（长期建设）

#### 9. 开局库（Opening Book）
**问题**：揭棋开局无参考，新手容易走出大劣开局。

**建议**：
- 收集高分局的前 15 步形成开局库
- 显示当前局面的开局名称和常见应手比例
- 标注"热门"、"稳健"、"陷阱"等标签

#### 10. 揭棋专用训练题库
**问题**：现有训练系统只有标准象棋题，没有揭棋题。

**建议**：
- 设计揭棋专属题型：
  - "最佳翻子选择"：给定局面，选择翻什么子最优
  - "暗子推理"：根据已知信息推理某暗子最可能是什么
  - "残局闯关"：从特定揭棋残局开始求胜
- 利用 SRS 间隔重复系统复习错题

#### 11. 揭棋棋谱社区
**问题**：棋谱保存在个人云端，缺乏分享和交流。

**建议**：
- 支持棋谱公开/分享链接
- 支持对棋谱评论、打分
- 精选棋谱推荐（名局、翻盘局、幸运局）

#### 12. 揭棋直播/观战
**问题**：揭棋作为娱乐性强的变体，适合观战但无此功能。

**建议**：
- 支持创建公开对局房间
- 观众可实时看到双方的暗子（上帝视角）
- 观众可发弹幕讨论"这里该翻什么"

---

### 🔵 P3 — 技术优化

#### 13. 暗子池验证与一致性检查
**问题**：当前没有验证 FEN 中暗子池数量是否与盘面暗子数量一致。

**建议**：
- 引入 `validationStatus` 计算属性（参考 JieqiBox）
- 检查：池中数量 ≥ 盘面上未揭暗子数
- 检查：各棋子类型总数不超过初始数
- 异常时显示警告，避免引擎分析错误局面

#### 14. FEN 格式兼容
**问题**：当前只支持自定义新格式，与 Pikafish 旧格式不兼容。

**建议**：
- 支持新旧格式互转（参考 JieqiBox 的 `convertFenFormat`）
- 导入时自动检测格式
- 导出时可选格式

#### 15. 揭棋引擎评测系统
**问题**：无法评估不同揭棋引擎的水平。

**建议**：
- 内置轻量级 tournament 系统（参考 JieqiBox 的 JAI）
- 让两个引擎自动对弈 N 局，输出 ELO 差值
- 可用于评估 WASM 引擎 vs 云端引擎 vs 本地引擎的相对强度

---

## 十、实施路线图建议

| 阶段 | 时间 | 目标 |
|------|------|------|
| **Phase 1** | 1-2 周 | P0 核心体验：暗子池调整、位置编辑器、棋谱跳转 |
| **Phase 2** | 2-3 周 | P1 分析增强：变着分析、吃子历史、运气指数 |
| **Phase 3** | 3-4 周 | P2 内容生态：开局库、揭棋题库、棋谱分享 |
| **Phase 4** | 持续 | P3 技术优化：验证系统、格式兼容、引擎评测 |

---

## 十一、总结

xiangqidao 的揭棋功能已经具备了**扎实的基础设施**（云端引擎、用户系统、跨平台支持、WASM 引擎），这些恰恰是 JieqiBox 所不具备的云端能力。但在**揭棋专用功能的深度**上，JieqiBox 明显更胜一筹——它的每个功能都经过揭棋场景的细致打磨（暗子池调整、吃子追踪、运气指数、Ponder、变着分析等）。

**建议的核心策略**是：
1. **保持云端优势**：继续发挥云端引擎、复盘库、用户系统的护城河
2. **补齐分析工具链**：参考 JieqiBox 的分析功能，但用 Web 技术做得更优雅
3. **差异化创新**：利用云端数据积累开局库、运气统计、AI 教练等 JieqiBox 做不到的功能
4. **社区化运营**：揭棋的娱乐性和运气成分天然适合社交传播，重点投入观战、分享、讨论
